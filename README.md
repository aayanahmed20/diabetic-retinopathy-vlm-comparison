# Diabetic Retinopathy Grading: Gemini vs. MedGemma vs. RetiZero

A comparative evaluation of three vision-language models on ICDR-graded diabetic
retinopathy severity classification (0–4), using the APTOS 2019 Blindness Detection
dataset as ground truth.

| Model | Type | Access |
|---|---|---|
| [Gemini](https://ai.google.dev/) | Hosted general-purpose multimodal API | API key |
| [MedGemma](https://huggingface.co/google/medgemma-4b-it) | Open-weight medical VLM (Google) | Hugging Face (gated, free) |
| [RetiZero](https://github.com/LooKing9218/RetiZero) | Open retinal foundation model, zero-shot | GitHub + manual weights download |

## Status

This repo contains a **complete, ready-to-run pipeline**, not pre-computed results.
`results/predictions.csv` and `results/metrics_summary.json` are generated by running
the notebook — they are not included here, because the numbers have to come from an
actual run against your own API/GPU access, not be invented.

## Repo structure

```
notebooks/DR_VLM_Comparison.ipynb   # run this in Google Colab (GPU runtime)
build_notebook.py                    # regenerates the notebook from source (nbformat)
reports/report_template.md           # fill in with real numbers after running the notebook
requirements.txt                     # for local/non-Colab use
```

## Running it

1. Open `notebooks/DR_VLM_Comparison.ipynb` in **Google Colab**
   (`File → Upload notebook`, or open directly from GitHub via `File → Open notebook → GitHub`)
2. `Runtime → Change runtime type → GPU` (T4 minimum — MedGemma and RetiZero need it)
3. Add these as **Colab secrets** (key icon, left sidebar) — never paste them into a cell:
   - `KAGGLE_USERNAME`, `KAGGLE_KEY` — kaggle.com → Account → API → Create New Token
   - `GEMINI_API_KEY` — https://aistudio.google.com/apikey
   - `HF_TOKEN` — https://huggingface.co/settings/tokens, and accept the license at
     https://huggingface.co/google/medgemma-4b-it with the same account
4. Run all cells top to bottom
5. Fill `reports/report_template.md` in with the numbers written to `results/`

## Known gaps to close before this is publication-ready

- **Sample size.** Default is `N_PER_GRADE = 2` (10 images), matching the original
  scoping request. That's a pipeline smoke test, not a result — for anything you intend
  to submit, raise it to 20–30 per grade (100–150 images) and report confidence intervals.
- **RetiZero inference (Section 6 of the notebook) is a documented skeleton, not tested
  code.** Its GitHub repo isn't a pip package — it ships a `Zeroshot.py` reference script
  and a `clip_modules/` folder rather than a stable public API, so the exact
  model-loading calls need to be confirmed against that script once you have GPU access
  to actually run and inspect it.
- **Ground truth caveat.** Published inter-ophthalmologist agreement on ICDR grading is
  kappa 0.40–0.65 — the "ground truth" here is one grader's label, not an infallible
  reference. Say so in the writeup.
- Gemini is a generalist model doing a specialist task the other two were trained for —
  note that asymmetry rather than presenting the comparison as three peers.

## Background: what's being graded

The **ICDR (International Clinical Diabetic Retinopathy) severity scale** grades fundus
photographs 0–4:

| Grade | Name | Description |
|---|---|---|
| 0 | No DR | No abnormalities |
| 1 | Mild NPDR | Microaneurysms only |
| 2 | Moderate NPDR | More than microaneurysms, less than severe |
| 3 | Severe NPDR | Extensive hemorrhages/venous beading/IRMA, no neovascularization yet |
| 4 | Proliferative DR (PDR) | Neovascularization and/or vitreous/preretinal hemorrhage |

Diabetic retinopathy is retinal blood vessel damage from prolonged high blood glucose,
and is a leading cause of vision loss in working-age adults worldwide. Risk factors
include diabetes duration, poor glycemic control, hypertension, and kidney disease
markers. Left untreated it can progress to diabetic macular edema, vitreous hemorrhage,
retinal detachment, and neovascular glaucoma.

## License / data use

APTOS 2019 data is subject to the Kaggle competition's own terms — do not redistribute
raw images in this repo. MedGemma and RetiZero each carry their own model licenses;
check both before any downstream/clinical use claim.
